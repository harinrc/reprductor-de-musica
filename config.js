window.DUO_CONFIG = {
  // Configuracion para el dueno del sitio (una sola vez).
  // Los usuarios finales no tienen que tocar nada.
  youtubeApiKey: "",
  // Si tienes backend publico, pon su base URL (ejemplo: https://mi-api.onrender.com)
  publicApiBase: "",
  // Catalogo libre de Jamendo (gratis en devportal.jamendo.com). Sin esto solo se usa Audius.
  jamendoClientId: "",
  // Last.fm (gratis en last.fm/api). Mejora mucho las recomendaciones "canciones similares".
  lastfmApiKey: "",
  // Pon false para usar solo YouTube.
  useFreeCatalogs: true,
  // Deezer / iTunes se usan como catalogo de referencia (que canciones existen y cuales
  // se parecen); el audio se resuelve despues en Audius, Jamendo o YouTube.
  useMetadataCatalogs: true,
  // Radios en vivo (radio-browser.info). Solo se aceptan streams https.
  useLiveRadio: true
};
